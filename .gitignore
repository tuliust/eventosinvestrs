node_modules/
dist/
build/
.cache/
.env*
!.env.example
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
coverage/
playwright-report/
test-results/
.claude-replay.*
.tmp-*

# Never version operational imports or files that may contain personal data.
src/imports/*.xlsx
src/imports/*.xls
src/imports/*.csv
src/imports/*.txt

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
